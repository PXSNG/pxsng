import TopBar from '@components/TopBar/TopBar';
import { memo } from 'react';

const LandingPage = () => {
  return (
    <div>
      <TopBar></TopBar>
    </div>
  );
};

export default memo(LandingPage);
